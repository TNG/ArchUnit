def scriptRoot = currentScriptRootOf this

productionProjects*.with {
    apply plugin: 'findbugs'

    findbugs {
        sourceSets = [sourceSets.main]
        excludeFilter = new File(scriptRoot, 'findbugs-excludes.xml')
    }

    tasks.withType(FindBugs) {
        reports {
            xml.enabled false
            html.enabled true
        }
    }
}