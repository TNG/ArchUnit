source "https://rubygems.org"

# commented the line below to get rid of github authentication warnings
# gem "github-pages", group: :jekyll_plugins

# added the following line, same reason as above
gem "jekyll", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-gist"
  gem "jekyll-feed"
  gem "jemoji"
#  gem "jekyll-asciidoc" # this doesn't work yet, since it's blacklisted on Github :-( We use Gradle Asciidoctor for now...
end
